# Source mappings

The source register and field matrix define the authoritative source policy. Detailed adapter mappings are maintained alongside the adapter implementation and must document source field, transformation, unit, context, locator and rights behavior.

The first mappings to implement are WFO, TAXREF, GROW, Practical Plants and CropGraph. Optional international sources are added only when a documented gap requires them.
